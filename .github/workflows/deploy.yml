name: Deploy via FTP

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.3

      - name: Setup Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version-file: .node-version
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Deploy to FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: 159.198.77.123
          username: "ownerpulse-frontend"
          password: "Zb9zced0303ouKNQ8zV0"
          local-dir: ./dist/
          server-dir: /
          delete-removed-files: true
          port: 21
          protocol: ftp
